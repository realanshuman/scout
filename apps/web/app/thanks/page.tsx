export default function Thanks() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center px-6 text-center">
      <div className="text-6xl">🎉</div>
      <h1 className="mt-6 text-4xl font-bold">Payment received!</h1>
      <p className="mt-4 max-w-md text-lg text-mist">
        Scout is generating your full investor report with personalized outreach
        right now. Check WhatsApp — it&apos;ll arrive in the next few minutes.
      </p>
    </main>
  );
}

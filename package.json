{
  "name": "scout",
  "private": true,
  "version": "0.1.0",
  "description": "Scout — your AI fundraising associate on WhatsApp",
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "build": "pnpm --filter \"./apps/*\" build",
    "lint": "pnpm --filter \"./apps/*\" lint",
    "seed:investors": "pnpm --filter @scout/api seed:investors"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0"
}
